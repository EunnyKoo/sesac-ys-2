<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%=age %>
    <%=name %>
    <!--ejs 태그 안에 오는 자바스크립트 코드는 한줄로 끝나야 함. -->
    <%  for(let i=0; i<hobby.length; i++) { %>
    <!-- ejs 태그 안에 바로 =이 오면 =뒤에 있는 변수나 값을 html로 노출시킨다.-->
        <div>취미 <%= hobby[i] %></div>
    <% } %>
    <!-- ejs 태그 안에 바로 =이 오면 =뒤에 있는 변수나 값을 html로 노출시킨다.-->    
    <%="hello eunny"%>

    <!-- ejs 태그 안에 바로 -이 오면 =뒤에 있는 변수나 값을 html로 노출시킨다.(html 태그를 인식)-->
    <%="<div>안녕</div>"%>
    <%-"<div>안녕</div>"%>  <!--div태그 파싱되서 나옴!-->

    <%-include("index.ejs") %>
    <%=include("index.ejs") %>

    <script>
        //백엔드에서 넘겨준 product라는 변수는 ejs 태그 내에서만 사용 가능
        console.log(hobby); //오류발생!
    </script>
</body>
</html>